TESSDATA_PREFIX=/usr/share/tesseract/tessdata/
UVICORN_WORKERS=2
UVICORN_RELOAD=True

# External model unloading for low VRAM systems
# Uncomment and configure to unload external models before Docling processing
# Requires DOCLING_SERVE_FREE_VRAM_ON_IDLE=true to be enabled

# DOCLING_SERVE_FREE_VRAM_ON_IDLE=true

# Advanced VRAM cleanup settings (optional, only effective when FREE_VRAM_ON_IDLE=true)
# DOCLING_SERVE_AGGRESSIVE_VRAM_CLEANUP=true
# DOCLING_SERVE_VRAM_CLEANUP_WARNING_THRESHOLD_MB=100.0
# DOCLING_SERVE_FORCE_GARBAGE_COLLECTION=true
# DOCLING_SERVE_CUDA_CONTEXT_RESET=false  # Experimental, may cause instability
# DOCLING_SERVE_MEMORY_LOGGING_ENABLED=true
# DOCLING_SERVE_CLEANUP_RETRY_ATTEMPTS=3
# DOCLING_SERVE_CLEANUP_RETRY_DELAY=1.0

# Ollama configuration
# DOCLING_SERVE_UNLOAD_OLLAMA_BASE_URL=http://localhost:11434
# DOCLING_SERVE_UNLOAD_OLLAMA_MODEL=llama3.2

# llama-swap configuration
# DOCLING_SERVE_UNLOAD_LLAMA_SWAP_BASE_URL=http://localhost:9292